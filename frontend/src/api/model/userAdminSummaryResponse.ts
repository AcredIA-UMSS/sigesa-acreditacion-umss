/**
 * Manual extension aligned with OpenAPI — regenerate with Orval when backend is up.
 */

export interface UserAdminSummaryResponse {
  userId?: string;
  email?: string;
  role?: string;
  status?: string;
  programIds?: string[];
}
