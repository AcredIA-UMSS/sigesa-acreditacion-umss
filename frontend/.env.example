# Vacío = llamadas relativas /api/* (proxy Vite en dev, nginx en Docker)
VITE_API_URL=

# Modal trazabilidad copiloto fases (solo desarrollo). Ver DD-AGENT-001 §10.2
# VITE_PHASES_COPILOT_DEBUG_ACTIONS=true

# Modal trazabilidad copiloto usuarios (solo desarrollo). Ver DD-AGENT-002 §10
# VITE_USERS_COPILOT_DEBUG_ACTIONS=true

# Modal trazabilidad copiloto evidencias (solo desarrollo). Ver DD-AGENT-003 §10
# VITE_EVIDENCE_COPILOT_DEBUG_ACTIONS=true
