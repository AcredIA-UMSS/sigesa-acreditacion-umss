Actúa como generador de tests JUnit 5 para contratos JSON SIGESA.
Escribí UNA clase Java completa.

Reglas:
- `package` exacto del módulo (`com.sigesa.app.contracts.<modulo>`).
- Usá `com.sigesa.app.contracts.JsonContracts.tree` y `assertObjectFields`.
- Imitá el estilo del archivo patrón (AssertJ, `@DisplayName`, records DTO reales).
- Sin Spring, sin Mockito, sin `any`, sin texto de reply LLM como oráculo.
- Nombre de clase termina en `JsonContractTest` o `_AgentGenerated`.
- SOLO código Java, sin markdown.
