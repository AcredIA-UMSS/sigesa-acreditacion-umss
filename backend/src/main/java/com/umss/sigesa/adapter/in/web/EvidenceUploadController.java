package com.umss.sigesa.adapter.in.web;

/**
 * Legacy stub from main's UC-004 draft.
 * Active controller: {@link EvidenceController} (Marlen / MOD-EVIDENCE).
 * Not a Spring bean — avoids duplicate mapping on {@code /api/v1/indicators/{id}/evidences}.
 */
public final class EvidenceUploadController {
    private EvidenceUploadController() {
    }
}
